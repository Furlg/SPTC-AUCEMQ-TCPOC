/**Spring-boot事务的学习**/
    <!--Transactional注解学习-->
        1:Transactional注解可以使用到类上面,也可以使用到方法上面。当作用到类上面时指的是此类的所有public修饰的方法都使用事务。
            作用到方法时指的是此方法进行事务管理。当类中的方法或者注册到方法级别时方法正确运行时spring会提交此事务。
        2:

/**事务的特性**/
    1:原子性:事务作为一个整体性被执行,对数据库中的数据进行操作时要么全部执行成功要么全部执行失败。也就意味着只要有一个失败那么所有的操作
        都应该是失败。
    2:隔离性:多个并发事务之间的执行不应该相互影响。
    3:持久性:已被提交的事务对数据库的修改应该是永久性的。
    4:一致性:一致性指的是什么？？？？？

/**并发执行数据库内的表时会出现的问题**/
        对数据库的表进行INSERT,UPDATE,DELETE时因为有一个COMMIT进行持久化操作。因此当INSERT,UPDATE,DELETE时没有进行COMMIT操作
            时此时并不是持久化操作。因此会出现以下问题。

            1: 